import styled from 'styled-components';

const Container = styled.div`
  select {
    background: #8fb8de;
    border: none;
    padding: 0.25em;
  }

  button {
    background: #8fb8de;
    border: none;
    padding: 0.25em;
  }
`;

export { Container };
